@model IEnumerable<Egresados.Models.AgregarOferta>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FechaInicio)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FechaInicio)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AgregarOfertaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AgregarOfertaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AgregarOfertaID })
        </td>
    </tr>
}

</table>
